<?xml version="1.0" encoding="utf-8"?>
<!--
  GeoMineral Trace — Analyze player chrome styles
  Viewport framing, transport icon buttons, scrubber track, waveform host.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Non-theme sizing (shared) -->
    <x:Double x:Key="GmtTransportButtonSizeShared">32</x:Double>
    <x:Double x:Key="GmtTransportIconSizeShared">14</x:Double>
    <x:Double x:Key="GmtTimelineScrubberHeightShared">12</x:Double>
    <x:Double x:Key="GmtTimelineWaveformHeightShared">52</x:Double>

    <!-- Icon transport button — Segoe Fluent Icons, compact NLE toolbar -->
    <Style x:Key="GmtTransportButton" TargetType="Button">
        <Setter Property="Width" Value="{StaticResource GmtTransportButtonSizeShared}" />
        <Setter Property="Height" Value="{StaticResource GmtTransportButtonSizeShared}" />
        <Setter Property="MinWidth" Value="{StaticResource GmtTransportButtonSizeShared}" />
        <Setter Property="MinHeight" Value="{StaticResource GmtTransportButtonSizeShared}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusSm}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{ThemeResource GmtBrushTransportIcon}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="Root"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition To="PointerOver" GeneratedDuration="0:0:0.15" />
                                    <VisualTransition To="Pressed" GeneratedDuration="0:0:0.15" />
                                    <VisualTransition To="Normal" GeneratedDuration="0:0:0.15" />
                                    <VisualTransition To="Disabled" GeneratedDuration="0:0:0.15" />
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Root"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource GmtBrushTransportHover}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource GmtBrushTransportPressed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Root"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.85"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </VisualState>
                                <!-- Disabled: muted fill + border + muted glyph (not opacity-only on primary blue).
                                     No PointerOver/Pressed storyboards apply while Disabled, so no hover feedback. -->
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource GmtBrushTransportDisabledFill}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource GmtBrushTransportDisabledBorder}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource GmtBrushTransportIconMuted}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Root"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.72"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Foreground="{TemplateBinding Foreground}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GmtTransportButtonPrimary" TargetType="Button" BasedOn="{StaticResource GmtTransportButton}">
        <Setter Property="Background" Value="{ThemeResource GmtBrushPrimary}" />
        <Setter Property="Foreground" Value="{ThemeResource GmtBrushTextEmphasis}" />
        <Setter Property="BorderBrush" Value="Transparent" />
    </Style>

    <Style x:Key="GmtTransportStrip" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource GmtBrushTransportSurface}" />
        <Setter Property="BorderBrush" Value="{ThemeResource GmtBrushTransportBorder}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <!-- Thickness literal — GmtSpace* doubles are for Spacing/ColumnSpacing only -->
        <Setter Property="Padding" Value="8" />
        <Setter Property="MinHeight" Value="44" />
    </Style>

    <Style x:Key="GmtScrubberTrack" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource GmtBrushTimelineTrack}" />
        <Setter Property="BorderBrush" Value="{ThemeResource GmtBrushBorderSubtle}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusXs}" />
        <Setter Property="Height" Value="{StaticResource GmtTimelineScrubberHeightShared}" />
    </Style>

    <Style x:Key="GmtWaveformHost" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="{StaticResource GmtTimelineWaveformHeightShared}" />
        <Setter Property="Opacity" Value="0.72" />
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusXs}" />
    </Style>

    <Style x:Key="GmtTimelineSelectionBand" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource GmtBrushTimelineSelection}" />
        <Setter Property="BorderBrush" Value="{ThemeResource GmtBrushTimelineSelectionBorder}" />
        <Setter Property="BorderThickness" Value="1,0" />
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusXs}" />
    </Style>

    <Style x:Key="GmtTimelineEvidenceMarker" TargetType="Rectangle">
        <Setter Property="Width" Value="2" />
        <Setter Property="RadiusX" Value="1" />
        <Setter Property="RadiusY" Value="1" />
        <Setter Property="Opacity" Value="0.92" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <Style x:Key="GmtKeyframeStripHost" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource GmtBrushBackgroundPanel}" />
        <Setter Property="BorderBrush" Value="{ThemeResource GmtBrushBorderSubtle}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="MinHeight" Value="88" />
    </Style>

</ResourceDictionary>
