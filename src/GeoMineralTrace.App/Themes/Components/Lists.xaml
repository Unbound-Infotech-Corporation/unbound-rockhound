<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="GmtListView" TargetType="ListView">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="IsItemClickEnabled" Value="True" />
    </Style>

    <Style x:Key="GmtListViewItem" TargetType="ListViewItem">
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusSm}" />
    </Style>

    <Style x:Key="GmtPageRoot" TargetType="Grid">
        <Setter Property="Background" Value="{ThemeResource GmtBrushBackgroundCanvas}" />
        <Setter Property="Padding" Value="{StaticResource GmtPagePadding}" />
    </Style>

    <Style x:Key="GmtInfoBar" TargetType="InfoBar">
        <Setter Property="CornerRadius" Value="{StaticResource GmtRadiusMd}" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

</ResourceDictionary>
